{-# LANGUAGE NoImplicitPrelude #-}

module Sixteen where

import Prelude
       (Bool(..), Maybe(..), (&&), (+), (++), (-), (||), concat, const,
        drop, filter, map, not, pi, reverse, take)

myId :: a -> a
myId a = a
