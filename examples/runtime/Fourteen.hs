{-# LANGUAGE NoImplicitPrelude #-}

module Fourteen where

import Prelude
       (Bool(..), Maybe(..), (&&), (+), (++), (-), (||), concat, drop,
        map, not, pi, reverse, take)

myId :: a -> a
myId a = a
