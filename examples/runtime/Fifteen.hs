{-# LANGUAGE NoImplicitPrelude #-}

module Fifteen where

import Prelude
       (Bool(..), Maybe(..), (&&), (+), (++), (-), (||), concat, drop,const,
        map, not, pi, reverse, take)

myId :: a -> a
myId a = a
